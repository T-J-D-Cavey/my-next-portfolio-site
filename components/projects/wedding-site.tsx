type ProjectItemComponentProps = {
    mode: string;
    modeStyles: Record<string, any>;
  };

  export default function WeddingSite({mode, modeStyles}: ProjectItemComponentProps) {

    return (
        <h3>Wedding site</h3>
    )
}