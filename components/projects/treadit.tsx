type ProjectItemComponentProps = {
    mode: string;
    modeStyles: Record<string, any>;
  };

  export default function Treadit({mode, modeStyles}: ProjectItemComponentProps) {

    return (
        <h3>Treadit</h3>
    )
}