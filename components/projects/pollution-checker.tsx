type ProjectItemComponentProps = {
    mode: string;
    modeStyles: Record<string, any>;
  };

  export default function PollutionChecker({mode, modeStyles}: ProjectItemComponentProps) {

    return (
        <h3>Pollution Checker</h3>
    )
}