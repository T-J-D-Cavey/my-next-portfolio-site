type ProjectItemComponentProps = {
    mode: string;
    modeStyles: Record<string, any>;
  };

  export default function RamenPopup({mode, modeStyles}: ProjectItemComponentProps) {

    return (
        <h3>Ramen Popup</h3>
    )
}